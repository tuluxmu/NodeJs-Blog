<% include header %>
<% include nav%>
<div class="tag-ul">
 <% posts.forEach(function (post, index) { %>
	  		
	  		
	  			<h3>
		  			<span class="label label-default">标题：
			  			<a href="/u/<%= post.name %>/<%= post.time.day %>/<%= post.title %>">
		  		 		<%= post.title %>
		  		 		</a>
		  			</span>
	  			</h3>
	  			
	  		
		  <p class="info">
		    作者：
		    <a href="/u/<%= post.name %>">
		    	<%= post.name %>
		    </a> 
		  </p>
		  
		  
  <% }) %>
  </div>
<% include foot%>